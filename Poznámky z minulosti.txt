KE ZKOPÍROVÁNÍ, POKUD SE BUDE NÌCO VKLÁDAT:

package papousek;

import java.util.Scanner;

public class Papousek {

    public static void main(String[] args)
    {
         Scanner sc = new Scanner(System.in, "Windows-1250");

    }
}


PLUS VLOŽIT import java.util.Arrays; POKUD POUŽÍVÁME ARRAYS


	PØEHLED

System.out.println("Ahoj.")						získáme text: Ahoj.
System.out.println("Výsledek souètu " + a + "+" + b + " je " + c + ".")	pokud a=1, b=2, c=a+b, pak získáme text: Výsledek souètu 1+2 je 3.
int a = 12								založí (definuje)	- promìnnou a (celé èíslo), která dostane hodnotu 12
													-- pro desetinná èísla: float (za èíslo nutno vložit F)
													-- pro øetìzec textu: String
int a = b + c								-			- promìnnou a (celé èíslo), která bude souètem b+c
int a = Integer.parseInt(sc.nextLine())					-			- promìnnou a (celé èíslo), které vznikne pøevedením Stringu
												  z následujícího øádku na èíslo
													-- pro float: Float.parseFloat(sc.nextLine())
													-- místo "sc.nextLine()" lze vložit název promìnné
	PRIMITIVNÍ DATOVÉ TYPY (zaèínají malým písmenem)
boolean vyraz = (15 > 5)						boolean nabyde hodnoty "true", protože 15 je vìtší než 5
										-- když tedy pod toto pøidáme System.out.println(vyraz), vŸískáme "true"
	REFERENÈNÍ DATOVÉ TYPY (zaèínají velkým písmenem)
String = "Jirka je super."						text "Jirka je super"
										-- na promìnnou Stringu mùžeme pøidat:
											--- s.startsWith("Jirka")   true/false,	jestli String "s" zaèíná "Jirka"
											--- s.endsWith("super.")		jestli String "s" konèí "super."
											--- s.contains("je")			jestli String "s" obsahuje "je"
s = s.toLowerCase()							pøedevede celý String "s" na malá písmena
s = s.toUpperCase()							-			  na velká písmena